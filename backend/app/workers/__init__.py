"""Trabalhos assíncronos do Atlas (§6.7).

- `queue` — registro do trabalho, backends de fila e execução;
- `tasks` — os executores de cada tipo de trabalho;
- `worker` — o processo que consome a fila.
"""
